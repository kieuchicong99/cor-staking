{
  "name": "cor-staking",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ kieuchicong99/cor-staking",
    "create-local": "graph create --node http://localhost:8020/ cor-staking",
    "remove-local": "graph remove --node http://localhost:8020/ cor-staking",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cor-staking"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
